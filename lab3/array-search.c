#include <stdio.h>
#include <stdlib.h>

int main(void){
	int my_array[] = {5, 2, 7, 3, 4};
	int max, min;

	max_min(my_array, 5, &max, &min);

	printf("max value is %d, min value is %d\n")
}